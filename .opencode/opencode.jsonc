{
  // Shared project config — portable across machines (macOS, Linux, WSL, Windows).
  // Machine-specific overrides belong in ~/.config/opencode/opencode.json (global),
  // which merges with this file per opencode's precedence rules.
  "$schema": "https://opencode.ai/config.json",
  "plugin": [
    ".opencode/plugins/graphify.js",
    "superpowers@git+https://github.com/obra/superpowers.git"
  ],
  "mcp": {
    "context7": {
      "type": "remote",
      "url": "https://mcp.context7.com/mcp"
    },
    "gh_grep": {
      "type": "remote",
      "url": "https://mcp.grep.app"
    },
    "memory": {
      "type": "local",
      "command": ["bun", "x", "@modelcontextprotocol/server-memory"],
      "enabled": true,
      "environment": {
        // {env:HOME} resolves on macOS/Linux/WSL/Git Bash.
        // On native Windows PowerShell/cmd use {env:USERPROFILE} — override in
        // your global config: ~/.config/opencode/opencode.json
        "MEMORY_FILE_PATH": "{env:HOME}/.local/share/opencode/memory.jsonl"
      },
      "timeout": 10000
    },
    "playwright": {
      "type": "local",
      "command": ["npx", "@playwright/mcp@latest"],
      "enabled": true,
      "timeout": 30000
    },
    "sequential-thinking": {
      "type": "local",
      "command": ["npx", "-y", "@modelcontextprotocol/server-sequential-thinking"],
      "enabled": true,
      "timeout": 10000
    }
  },
  // Enforces the advisory operating mode declared in AGENTS.md.
  // Prose in AGENTS.md is a request; this block is the actual boundary.
  // NOTE: within each object the LAST matching rule wins — broad patterns
  // first, narrow exceptions after.
  "permission": {
    "edit": {
      "*": "allow",
      // Application source and infrastructure — the agent advises, you implement.
      "backend/app/**": "deny",
      "backend/pyproject.toml": "deny",
      "backend/uv.lock": "deny",
      "backend/Dockerfile": "deny",
      "backend/alembic.ini": "deny",
      "backend/migrations/**": "deny",
      "docker-compose.yml": "deny",
      "docker/**": "deny"
    },
    "bash": {
      "*": "allow",
      // History-rewriting or destructive operations always confirm first.
      "git commit*": "ask",
      "git push*": "ask",
      "git reset*": "ask",
      "git checkout*": "ask",
      "git rm*": "ask",
      "rm *": "ask",
      "docker compose down*": "ask",
      "psql*": "ask",
      "uv add*": "ask",
      "uv remove*": "ask"
    }
  }
}
